
function init() {
	local DIR="${BASH_SOURCE%/*}"
	if [[ ! -d "$DIR" ]]; then DIR="$PWD"; fi
	source "$DIR/require.sh"
}
init

import list

things=($(list))

for thing in ${things[@]} ; do
	require $thing
done
