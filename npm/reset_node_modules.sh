import file.walk_up
import user.ask

function npm.reset_node_modules() {
	local dir="$(walk_up package.json)"
	pushd $dir >> /dev/null

	if user.ask 'Do you want to remove package-lock.json?' ; then
		rm package-lock.json -v
	fi

	rm node_modules/ -rf
	npm install

	popd >> /dev/null
}
