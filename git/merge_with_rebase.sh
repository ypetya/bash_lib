# take two branch patterns : branch A, branch B. 
# Execute the following steps:
# Update from remotes
# Let the user to select branches
# Interactive rebase B on A
# checkout A, merge with fast forward ( ~ is it equal with a reset ? )
# if no branch specified A can be the current branch
